[supervisord]
nodaemon=true
user=root
logfile=/dev/stdout
logfile_maxbytes=0
pidfile=/var/run/supervisord.pid

; --- Syncthing: p2p sync with the office PC. GUI bound to localhost only,
; never exposed outside the container/host (see docker-compose.yml).
[program:syncthing]
command=/usr/bin/syncthing serve --no-browser --no-restart --gui-address=0.0.0.0:8384 --home=/data/syncthing-config
autostart=true
autorestart=true
stopsignal=INT
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0

; --- The Flask upload form, served by gunicorn on 127.0.0.1:8000.
; A host-level Nginx (outside this container) reverse-proxies HTTPS to it.
[program:webapp]
command=gunicorn --bind 0.0.0.0:8000 --workers 2 --timeout 120 app:app
directory=/app
autostart=true
autorestart=true
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0

; --- Deletes submission folders older than MAX_AGE_DAYS on a loop.
[program:cleanup]
command=/scripts/cleanup_loop.sh
autostart=true
autorestart=true
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
